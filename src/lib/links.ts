/**
 * Every off-page destination the landing page points at, in one file.
 *
 * Two of these are real and two are placeholders. PRODUCT.md keeps the funding
 * model, the licence identifier and the launch date deliberately undecided, and
 * the repository URL and the offset partner have not been named — so they are
 * marked here rather than guessed at in six templates. Anything still reading
 * `PLACEHOLDER` renders a dead `#` link and needs wiring before launch.
 */

/** Real. The model provider's own retention terms, cited on the page. */
export const PROVIDER_TRUST_URL = "https://trust.novita.ai"

/** Real. The provider named in the copy, so the citation is checkable. */
export const PROVIDER_NAME = "Novita"

/** PLACEHOLDER — the repository is not published under a URL yet. */
export const SOURCE_URL = "#"

/** PLACEHOLDER — no waitlist endpoint exists. Wire before the form can post. */
export const WAITLIST_ACTION = "#"

/** PLACEHOLDER — the carbon offset partner is not named yet. */
export const OFFSET_PARTNER_URL = "#"

/** PLACEHOLDER — legal pages are not written. */
export const TERMS_URL = "#"
export const PRIVACY_URL = "#"
