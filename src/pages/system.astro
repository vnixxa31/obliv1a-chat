---
import Erasure from "@/components/system/Erasure.astro"
import Grounds from "@/components/system/Grounds.astro"
import Opening from "@/components/system/Opening.astro"
import Layout from "@/layouts/main.astro"

// The worked example: `obliv1a-brand/starter.html` rebuilt in Astro, kept as the
// reference for how the system composes here. The marketing page is `index.astro`;
// this one exists to be read next to `starter.html`, not to be linked from it.
---

<Layout
  title="Obliv1a — the system, installed"
  description="The Obliv1a identity system, installed: the ground contract, the erasure, and shadcn components restyled to read both."
>
  <Opening />
  <Grounds />
  <Erasure />
</Layout>
